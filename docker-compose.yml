services:
  mosca-insights:
    build: .
    ports:
      - '3020:3000'
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - AUTH_USER=admin
      - AUTH_PASS=mosca2026
      - AUTH_SECRET=mosca-insights-prod-secret
    restart: unless-stopped
